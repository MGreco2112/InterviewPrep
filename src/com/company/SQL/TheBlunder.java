package com.company.SQL;

public class TheBlunder {
//-- SELECT ROUND(SUM(salary) / COUNT(*)) FROM employees;
//
//-- SELECT ROUND(SUM(REPLACE(salary, 0, "")) / COUNT(*)) FROM employees;
//
//SELECT ROUND(SUM(salary) / COUNT(*)) - ROUND(SUM(REPLACE(salary, 0, "")) / COUNT(*)) FROM employees;
}
